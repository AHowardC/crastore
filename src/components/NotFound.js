import React from "react";

const NotFound = () => (
  <div className="alert alert-warning">
    <h4 className="alert-heading">Not Found</h4>
    <p className="mb-0">This page does not exist</p>
  </div>
);

export default NotFound;
